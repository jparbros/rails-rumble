.row-fluid
  .span12.data-block
    .data-container
      %header
        %h2
          New Campaign
      %section
        = form_for @campaign, :html => {class: "form-horizontal"} do |form|
          = render partial: "form", locals: {form: form}
