%tr
  %td= incident_type.id
  %td= incident_type.name
  %td
    = link_to "edit", edit_incident_type_path(incident_type), class: "btn btn-primary"
    = link_to "delete", incident_type_path(incident_type), method: :delete, confirm: "Are you sure?", class: "btn btn-danger"
