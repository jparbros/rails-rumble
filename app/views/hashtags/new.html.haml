.row-fluid
  .span12.data-block
    .data-container
      %header
        %h2
          New Hashtag
      %section
        = form_for [@campaign, @hashtag], :html => {class: "form-horizontal"} do |form|
          = render partial: "form", locals: {form: form}
