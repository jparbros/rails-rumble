.row-fluid
  .span12.data-block
    .data-container
      %header
        %h2
          Edit Incident
      %section
        = form_for [@campaign, @incident], :html => {class: "form-horizontal"} do |form|
          = render partial: "form", locals: {form: form}