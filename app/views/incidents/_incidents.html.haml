%tr
  %td= incident.id
  %td= incident.name
  %td= incident.description
  %td
    = link_to "edit", edit_campaign_incident_path(@campaign, incident), class: "btn btn-primary"
    = link_to "delete", campaign_incident_path(@campaign, incident), method: :delete, confirm: "Are you sure?", class: "btn btn-danger"
