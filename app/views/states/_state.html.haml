%tr
  %td= state.id
  %td= state.name
  %td
    = link_to "edit", edit_state_path(state), class: "btn btn-primary"
    = link_to "delete", state_path(state), method: :delete, confirm: "Are you sure?", class: "btn btn-danger"
