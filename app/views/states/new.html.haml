.row-fluid
  .span12.data-block
    .data-container
      %header
        %h2
          New State
      %section
        = form_for @state, :html => {class: "form-horizontal"} do |form|
          = render partial: "form", locals: {form: form}
